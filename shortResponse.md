# Short Response — LG 7.0: Responsive Units

Answer each question in 2–3 sentences. 

---

## Question 1 — Describe

What is the difference between a static unit like `px` and a responsive unit like `%` or `vh`?

Describe what makes a unit responsive and why that matters when building a website.

the difference between a static unit like px and a responsive unit like % or vh is that the px allows the images size or text size to change however the percentage or the vh allows it to have the same format no matter where  you move the screen snd it allows it to specifically take up how much ever space it needs 








---

## Question 2 — Explain

Look at these two CSS rules:

```css
.image {
  width: 400px;
}

.image {
  width: 50%;
}
```

Explain what happens to the image on a small screen with each rule. Why does one behave better than the other?

the .image tag would response better because if the width has a percentage it allows us to see how much space it could take up no matter where the screen is and it does not change the formatting but with px it tends to change and has a different sizing and it does not remain the same when the screen is moved to other places which creates a problem in the sizing of the tag and it messes it up so in coding its always better to use the percentage since it takes up the appoiate sizing git





